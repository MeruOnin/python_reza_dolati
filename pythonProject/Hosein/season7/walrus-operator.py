# گرفتن ورودی اسم کاربر و ذخیره در یک لیست
# list1 = []
# while (s := input("name (e for exit): ")) != "e":
#     list1.append(s)
#
# print(list1)

# دیکشنری

x = [1, 2, 3, 4]
d = {
    "l": (length := len(x)),
    "s": (Sum := sum(x)),
    "a": (ave := Sum / length)
}
